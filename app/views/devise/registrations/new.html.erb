<div class="espacio-navbar">
</div>
<div class="background-perfil">
</div>

<div class='container-register'>
  <div class="title-perfil text-center">
    <h1>Primero completa el siguiente formulario</h1>
  </div>
  <div class="form-register">
    <h2>Datos personales</h2>

    <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
      <%= f.error_notification %>

      <div class="form-inputs grid-form">
          <%= f.input :first_name,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "first_name" },
                      label: false,
                      placeholder: 'Nombre' %>
          <%= f.input :last_name,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "last_name" },
                      label: false,
                      placeholder: 'Apellido' %>
          <%= f.input :university,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "university" },
                      label: false,
                      placeholder: 'Universidad' %>
          <%= f.input :level_instruction,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "level_instruction" },
                      label: false,
                      placeholder: 'Nivel de instrucción' %>
          <%= f.input :phone,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "phone" },
                      label: false,
                      placeholder: 'Celular' %>
          <%= f.input :email,
                      required: true,
                      autofocus: true,
                      input_html: { autocomplete: "email" },
                        label: false,
                        placeholder: 'Email' %>
          <%= f.input :password,
                      required: true,
                      input_html: { autocomplete: "new-password" },
                        label: false,
                       placeholder: 'Contraseña' %>

        <%= f.input :password_confirmation,
                    required: true,
                    input_html: { autocomplete: "new-password" },
                      label: false,
                      placeholder: 'Confirmar contraseña'  %>
      </div>

      <div class="form-actions">
        <%= f.button :submit, "Registrarse", class: 'btn-registro' %>
      </div>
    <% end %>

  </div>
</div>
